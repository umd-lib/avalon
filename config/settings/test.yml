domain:
  host: test.host
  # UMD Customization
  # Override UMD-customized "port: 3000", in "settings.yml"
  port: 80
  # End UMD Customization
dropbox:
  upload_uri: smb://test.host/dropbox
auth:
  configuration:
    - :name: Avalon Lti OAuth
      :provider: :lti
      :hidden: true
      :params:
        :oauth_credentials:
          key: 'secret'
encoding:
  engine_adapter: test
email: 
  mailer:
